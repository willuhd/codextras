{
  "name": "codextras",
  "version": "0.0.5",
  "private": true,
  "type": "module",
  "description": "Custom dependency-free Codex router with compaction, subagent, tool call, advertised vision, and preserved reasoning support",
  "scripts": {
    "start": "bun run src/server.mjs",
    "build": "bun build --compile --outfile codextras src/cli.mjs",
    "test": "bun test",
    "smoke": "bun run smoke/smoke.mjs"
  }
}